---
name: {{ name }}
description: {{ description }}
---

# {{ name }}

{{ description }}
{% if triggers %}

Use when the user says: {{ triggers | join(", ") }}.
{% endif %}

## Procedure
{% if usage %}

{{ usage }}
{% endif %}
{% if mcp_servers %}

Wraps {{ mcp_servers | length }} MCP server(s):
{% for s in mcp_servers %}
- `{{ s.server }}` (transport: {{ s.transport }}) tools: {% for t in s.tools %}`{{ t }}`{% if not loop.last %}, {% endif %}{% endfor %}

{% endfor %}
{% endif %}
{% if cli_cmds %}

Run the bundled script(s):
{% for c in cli_cmds %}
```
{{ c }}
```
{% endfor %}
{% endif %}
{% if model_tier %}

**Model tier: {{ model_tier }}** (advisory — dispatch judgment-heavy steps to a `{{ model_tier }}`-tier agent).
{% endif %}

## Guardrails

{% for g in guardrails %}
- {{ g.text }}
{% endfor %}
